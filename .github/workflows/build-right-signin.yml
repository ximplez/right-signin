name: Build-right-signin

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "*.md"
      - ".github/workflows/**"
      - ".gitignore"
      - ".git/**"
      - "LICENSE"
      - "runtime/**"
    branches:
      - main

env:
  RELEASE_BRANCH: release
  BINARY_NAME: right-signin_linux_amd64
  RELEASE_REMOTE: https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          path: build

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: build/go.mod

      - name: Build right-signin binary
        run: |
          cd build || exit 1
          mkdir -p dist
          GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o "dist/${BINARY_NAME}" .

      - name: Prepare release payload
        run: |
          mkdir -p release
          cp "build/dist/${BINARY_NAME}" "release/${BINARY_NAME}"

      - name: Commit release branch artifacts
        run: |
          cd release || exit 1
          git init
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git checkout --orphan "${RELEASE_BRANCH}"
          git remote add origin "${RELEASE_REMOTE}"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "build right-signin"
          git push -f origin "${RELEASE_BRANCH}"

      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 3
          keep_minimum_runs: 8
